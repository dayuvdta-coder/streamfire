# Server Configuration
PORT=7575
NODE_ENV=production
PUBLIC_IP=127.0.0.1
# Optional in container deploy. If empty, app generates runtime secret automatically.
SESSION_SECRET=
HTTP_REQUEST_LOGS=0

# Optional. If empty, app will fallback to auto runtime paths.
UPLOAD_PATH=./public/uploads
DB_PATH=./db/streamfire.db
FFMPEG_PATH=/usr/bin/ffmpeg
# PLAYWRIGHT_BROWSERS_PATH=/opt/streamfire/.cache/ms-playwright
IG_GO_LIVE_WAIT_MS=180000
MISTRAL_API_KEY=
MISTRAL_MODEL=mistral-small-latest
